spring:
  application:
    name: BurgerStore [Product Service]
  #setting for this service
  config:
    import: ongpobg33
      

endpoint:
  path: ${codycod.uri}/products

database:
  name: db_products_service

server:
  port: 8085
