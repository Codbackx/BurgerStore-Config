spring:
  application:
    name: BurgerStore [Product Service]
  config:
    import: ${configserver.import.uri}/database-configuration
      

endpoint:
  path: ${codycod.uri}/products

database:
  name: db_products_service

server:
  port: 8085
